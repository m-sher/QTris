# Copy to .env and fill in. Used by scripts/publish_leaderboard.py.
# .env is gitignored; never commit your real token.
LEADERBOARD_URL=https://qtris-leaderboard.<subdomain>.workers.dev
LEADERBOARD_TOKEN=your-publish-secret
